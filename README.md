# Video Stream Links Processor

This repository automatically processes YouTube video links and generates streamable URLs in JSON format using GitHub Actions.

## Setup

1. **Fork this repository** to your own GitHub account

2. **Add your video links** to `links.txt` (one URL per line):
